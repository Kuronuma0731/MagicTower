using System;
using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class FloorControl : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField] private GameObject FloorControlStats;
    void Start()
    {
        //Action(init);
        //init();
        //GetAllFloorObject(FloorControlStats.transform);
    }
    // Get GameObject Name in the Floor
    
}
